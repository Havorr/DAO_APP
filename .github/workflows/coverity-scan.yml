name: Coverity Scan (Python)

on:
  workflow_dispatch:
  push:
    branches: [ "main" ]

jobs:
  coverity:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Download Coverity
        env:
          COVERITY_SCAN_TOKEN: ${{ secrets.COVERITY_SCAN_TOKEN }}
        run: |
          set -e
          curl -fL \
            -X POST \
            --data "token=${COVERITY_SCAN_TOKEN}&project=DAO_APP" \
            https://scan.coverity.com/download/linux64 \
            -o coverity.tgz

          mkdir coverity
          tar -xzf coverity.tgz -C coverity --strip 1

      - name: Capture Python sources
        run: |
          set -e
          ./coverity/bin/cov-capture-python \
            --dir cov-int \
            Aplikacja

      - name: Package capture
        run: |
          tar czf cov-int.tgz cov-int

      - name: Submit to Coverity Scan
        run: |
          curl -fL \
            --form token=${{ secrets.COVERITY_SCAN_TOKEN }} \
            --form email=${{ secrets.COVERITY_SCAN_EMAIL }} \
            --form file=@cov-int.tgz \
            --form version="$(git rev-parse HEAD)" \
            --form description="Python capture via cov-capture-python" \
            "https://scan.coverity.com/builds?project=DAO_APP"
